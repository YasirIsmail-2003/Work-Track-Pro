### Example environment variables for Render
# Copy (and rename) this file to `server/.env` locally or set these variables in the Render dashboard

# Supabase (public keys for client usage)
NEXT_PUBLIC_SUPABASE_URL="https://your-supabase-url.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Supabase server role (KEEP SECRET) - use Render "Environment" / "Secrets"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Database (Postgres) - if using a managed DB on Render or elsewhere
DATABASE_URL="postgres://user:password@hostname:5432/dbname"

# SMTP (nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass

# App settings
APP_URL=https://your-production-domain.example
TZ=Asia/Kolkata

# Any other secrets your app requires
# EXAMPLE_OTHER_SECRET=replace_with_value

# Notes:
# - Do NOT commit real secret values to git.
# - On Render, add SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, SMTP_PASS, etc. as Environment / Secrets.
